FROM python:3.11-slim

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends \
    curl \
    && rm -rf /var/lib/apt/lists/*

COPY apps/bot/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY apps/bot /app

CMD ["python", "main.py"]
